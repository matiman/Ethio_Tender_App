<h1>Listing tenders</h1>

	<table border="0">
		<tr><td> Filter by status &nbsp; </td>
			<td>
			  <select>
			    <option value="Initiated">Initiated</option>
			    <option value="Closed">Closed</option>
			    <option value="Won">Won</option>
			    <option value="Lost">Lost</option>
		    	  </select>
		  </td></tr>
		
		


	</table>

<table border="1">
  <tr>
    <th>Title</th>
    <th>Buyer Name</th>
    <th>Remaining dates</th>
    <th>Status</th>
    <th>Bid amount</th>
    <%#   <th>Description</th> %>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @tenders.each do |tender| %>
  <tr>
    <td><%= tender.title %></td>
    <td><%= tender.buyer_name %></td>
    <td><%= distance_of_time_in_words(tender.opening_date,tender.closing_date) %></td>
    <td><%= tender.status %></td>
    <td><%= tender.bid_amount %></td>

    <%#  tender.description %>

    <td><%= link_to 'Show', tender %></td>
    <td><%= link_to 'Edit', edit_tender_path(tender) %></td>
    <td><%= link_to 'Destroy', tender, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Tender', new_tender_path %>
